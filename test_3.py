import numpy as np

np.random.seed(0)  # 设置种子
random_numbers = np.random.uniform(low=0.0, high=1.0, size=5)  # 生成随机数
random_numbers2 = np.random.uniform(low=0.0, high=1.0, size=5)  # 生成随机数
print(random_numbers)
